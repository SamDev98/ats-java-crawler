# ===========================================
# ATS Java Crawler v0.4.1 - OTIMIZADO
# ===========================================

# Server
server.port=8080

# Database
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/ats}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:postgres}
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.show-sql=false

# HTTP Client
http.connect-timeout=10000
http.socket-timeout=15000
http.max-retries=2

# Crawler
crawler.cron-expression=${CRON_EXPRESSION:0 0 7 * * *}
crawler.cron-zone=${CRON_ZONE:UTC}
crawler.expiry-days=${EXPIRY_DAYS:15}

# ============================================
# ? ATS FUNCIONANDO (baseado nos logs)
# ============================================

# ? Greenhouse - 803 jobs fetched (EXCELENTE!)
crawler.greenhouse-companies=${ATS_GREENHOUSE:robinhood,mercury,intercom,mixpanel,amplitude,poshmark,retool,webflow,lattice,ebanx,c6bank,stabilityai,marqeta,vercel}

# ? Ashby - 10 companies parsed (BOM)
crawler.ashby-companies=${ATS_ASHBY:ramp,notion,snowflake,shopify,cohere,characterai,jasper,buffer,helpscout,linear}

# ? BreezyHR - 4 jobs (BOM)
crawler.breezy-companies=${ATS_BREEZY:zapier,hubstaff,helloalice,everlaw}

# ? Recruitee - 6 jobs (BOM)
crawler.recruitee-companies=${ATS_RECRUITEE:timedoctor}

# ? Jobvite - 0 jobs mas funciona (MANTER)
crawler.jobvite-companies=${ATS_JOBVITE:instacart,doordash}

# ? DESABILITADOS (problemas identificados nos logs)

# Workable - 404 Not Found em todas companies
crawler.workable-companies=

# Lever - 401 Unauthorized (precisa autenticação)
crawler.lever-companies=

# BambooHR - Cloudflare block 403 Forbidden
crawler.bamboohr-companies=

# Teamtailor - 404 Not Found (company não existe)
crawler.teamtailor-companies=

# Zoho - Não configurado
crawler.zoho-companies=

# Taleo - Não configurado
crawler.taleo-companies=

# Rippling - Não configurado
crawler.rippling-companies=

# RecruitCRM - Não configurado
crawler.recruitcrm-companies=

# Jobsoid - Não configurado
crawler.jobsoid-companies=

# Talentlyft - Não configurado
crawler.talentlyft-companies=

# 100Hires - Não configurado
crawler.hundredHires-companies=

# Recruiteze - Não configurado
crawler.recruiteze-companies=

# Kula - Não configurado
crawler.kula-companies=

# iSmart - Não configurado
crawler.ismart-companies=

# Vincere - Não configurado
crawler.vincere-companies=

# ============================================
# FILTROS - Java + Remote/LatAm
# ============================================
filter.role-keywords=${ROLE_KEYWORDS:java,spring,spring boot,kotlin,microservices,backend,jvm}
filter.include-keywords=${INCLUDE_KEYWORDS:remote,work from anywhere,remote anywhere,latam,latin america,brazil,brasil,worldwide,global,international}
filter.exclude-keywords=${EXCLUDE_KEYWORDS:us only,usa only,united states only,on-site,onsite,in-office}

# ============================================
# GOOGLE SHEETS - HABILITADO
# ============================================
google.sheets.enabled=${SHEETS_ENABLED:true}
google.sheets.spreadsheetId=${GOOGLE_SHEETS_ID:1_WHrO9_T48OXcQVm8J2sfobyEih4pcJsesTF1N2QeDM}
google.credentials.path=${GOOGLE_APPLICATION_CREDENTIALS:src/main/resources/credentials.json}

# ============================================
# DISCORD
# ============================================
discord.webhook=${DISCORD_WEBHOOK:}

# ============================================
# LOGGING
# ============================================
logging.level.root=INFO
logging.level.com.atscrawler=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN

# ============================================
# PERFORMANCE
# ============================================
spring.jpa.properties.hibernate.jdbc.fetch_size=50
spring.jpa.properties.hibernate.cache.use_second_level_cache=false
spring.jpa.properties.hibernate.cache.use_query_cache=false
spring.jpa.open-in-view=false
spring.main.lazy-initialization=false